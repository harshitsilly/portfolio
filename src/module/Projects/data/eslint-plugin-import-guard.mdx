import { Text, Box, Link } from '../../../atoms';
import { setUrl } from '../../../utils';

-   ESLint plugin to enforce architectural boundaries in micro-frontend platforms.

-   Prevents unauthorized imports between modules, ensuring clean separation of concerns.

-   Configurable rule sets to define allowed and restricted import patterns.

-   Helps maintain scalable and maintainable codebases in large monorepos.

<br />

<Text align="left" bold>
	Tech Stack
</Text>

-   ESLint
-   JavaScript/TypeScript
-   AST Manipulation
