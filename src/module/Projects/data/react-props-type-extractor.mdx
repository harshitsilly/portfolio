import { Text, Box, Link } from '../../../atoms';
import { setUrl } from '../../../utils';

-   Extracts React component prop types at build time.

-   Generates TypeScript interfaces or PropTypes documentation automatically.

-   Improves code maintainability by keeping prop types in sync with component implementation.

-   Open sourced tool used in CI/CD pipelines for type safety.

<br />

<Text align="left" bold>
	Tech Stack
</Text>

-   React
-   TypeScript
-   Babel/Webpack
